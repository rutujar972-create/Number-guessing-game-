# Number-guessing-game-
User guesses a random number generated by computer. And then user wins

import random 
random_num = random.int(1,10)
guess = 0
while guess != random_num :
  guess = user_guess =                       int(input("enter a 
          number fron 1 to 10"))

  if (guess > random_num) :
     print( "too high, try again")
  elif (guess < random_num):
     print("too low, try again")
  else:
     print("congratulations you.        won!")

   
